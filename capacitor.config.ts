import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pablo.pokedex',
  appName: 'Pokedex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
